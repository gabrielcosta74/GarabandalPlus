import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'My Orders',
  description: 'View your store purchase history.',
};

export { default } from '../../encomendas/page';
